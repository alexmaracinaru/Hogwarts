const arr = ["a", "b"];

function displayList(something, house, gender) {
  return house + gender;
}

displayList(arr, "Gryffindor", "male");

const a = 5;
const b = 6;

const addNumbers = (a, b) => {
  return a + b;
};

addNumbers();
addNumbers();

addNumbers(1, 2);

addNumbers(5, 2);
